{
  "name": "everaura-creations-mern",
  "version": "2.0.0",
  "description": "EverAura Creations — Business Website (MERN Stack with Image Sliders)",
  "private": true,
  "scripts": {
    "postinstall": "npm --prefix backend install --include=dev && npm --prefix frontend install --include=dev",
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"bgYellow.bold,bgCyan.bold\" \"npm run server\" \"npm run client\"",
    "server": "npm --prefix backend run dev",
    "client": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start": "node backend/server.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
